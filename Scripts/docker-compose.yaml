services:

  backend:
    build:
      context: .
      dockerfile: Dockerfile
        #    image: macnaer/backend
    ports:
      - 5034:5034
    restart: always

  frontend:
    image: macnaer/frontend
    ports:
      - 80:80
    restart: always
